# Parcela API (required for bookings to appear on staff dashboards)
# On Expo Go (phone), localhost is auto-replaced with your PC's LAN IP from Metro.
# If bookings still don't sync, set your PC's IP explicitly:
# EXPO_PUBLIC_DEV_API_HOST=192.168.1.100
EXPO_PUBLIC_API_URL=http://localhost:3002/api
