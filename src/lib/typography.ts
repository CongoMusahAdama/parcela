/**
 * Typography rules
 *
 * Courier New (`font-display`) — headings and prominent UI
 *   Use for: headings, buttons, badges, section labels, brand wordmark
 *
 * Inter (`font-body`) — reading text and form fields
 *   Weights: 400 Regular, 500 Medium
 *   Use for: paragraphs, subtitles, inputs, selects, labels, helper text
 */

export const typography = {
  display: "font-display",
  body: "font-body",
} as const;
