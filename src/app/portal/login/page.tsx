import { OperatorLoginView } from "@/components/operator/OperatorLoginView";

export const metadata = {
  title: "Operator sign in — Parcela",
  description: "Sign in for station staff and branch leads at your transport terminal.",
};

export default function OperatorLoginPage() {
  return <OperatorLoginView />;
}
