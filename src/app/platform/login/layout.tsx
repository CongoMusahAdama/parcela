export const metadata = {
  title: "Sign in",
  description: "Parcela internal platform control for onboarding transport operators.",
};

export default function PlatformLoginLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
