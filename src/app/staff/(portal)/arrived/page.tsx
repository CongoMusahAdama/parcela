import { StaffArrivedView } from "@/components/staff/StaffArrivedView";

export const metadata = { title: "Arrived — Parcela Staff" };

export default function StaffArrivedPage() {
  return <StaffArrivedView />;
}
