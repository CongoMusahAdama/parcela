import { StaffCollectionView } from "@/components/staff/StaffCollectionView";

export const metadata = { title: "Ready to collect — Parcela Staff" };

export default function StaffCollectionPage() {
  return <StaffCollectionView />;
}
